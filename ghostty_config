# https://ghostty.org/docs/config

# 1) 字体：先把观感拉满（最值回票价）
font-family = "JetBrainsMono Nerd Font Mono"
# 中文 fallback 字体（需先安装：brew install --cask font-lxgw-wenkai）
# font-family = "LXGW WenKai"

# 窗口启动时最大化 (需要 Ghostty 1.1.0+)
maximize = true
font-size = 12

# 2) 主题：一行切换（并支持深浅色）
theme = dark:Catppuccin Frappe,light:Catppuccin Latte

# 4) 半透明 + 背景模糊（按需；喜欢就开）
background-opacity = 0.9
background-blur = true

# 5) Shell 集成：让“新 tab/split 继承目录、jump_to_prompt”等更顺滑
shell-integration = detect

# 6) SSH/Terminfo + 禁止 shell 集成修改光标
shell-integration-features = ssh-terminfo,ssh-env,no-cursor

# macOS 专属：消除标题栏，更加极简
macos-titlebar-style = hidden

# 8) 快捷终端：瞬间弹出，无动画
quick-terminal-animation-duration = 0

# 9) 光标：方块样式，不闪烁
cursor-style = block
cursor-style-blink = false

# 10) 选中即复制：选中文本自动复制到系统剪贴板（可用 Cmd+V 粘贴）
copy-on-select = clipboard

# 适合 Claude Code 等长对话应用
scrollback-limit = 200000000

# option+. 获取上一个参数
keybind = alt+.=esc:.
