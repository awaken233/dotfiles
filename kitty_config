# https://sw.kovidgoyal.net/kitty/conf.html

# 1) Fonts
font_family      JetBrainsMono Nerd Font Mono
font_size        12.0

# 2) Theme Configuration
# Light: Catppuccin Latte
# Dark:  Catppuccin Frappe
# Auto-switch mechanism:
# Kitty (0.38+) automatically loads 'light-theme.auto.conf' or 'dark-theme.auto.conf'
# based on the system's appearance setting. These files are symlinked by the dotfiles installer.

# 3) Window Layout
remember_window_size  yes
initial_window_width  640
initial_window_height 400
os_window_state       maximized
hide_window_decorations titlebar-only

# 4) Appearance
background_opacity 0.9
background_blur 20

# 5) Shell Integration
# "no-cursor" 禁用了 shell (zsh) 改变光标形状的序列，以防止干扰 NeoVim 等应用
# NeoVim 会通过 tmux 转发的序列独立控制光标
shell_integration enabled no-cursor

# 7) Copy on Select
copy_on_select yes

# macOS Specifics
macos_option_as_alt yes
macos_quit_when_last_window_closed yes

# Cool but useless, will add an extra 200MB of memory footprint
# cursor_trail 3
# cursor_trail_decay 0.1 0.4

# 把 kitty_mod+Y 设为“复制上一个命令输出到剪贴板”
map kitty_mod+y copy_last_command_output

